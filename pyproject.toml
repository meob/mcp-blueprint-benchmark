[project]
name = "mcp-blueprint-benchmark"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "jinja2>=3.1.6",
    "mcp>=1.27,<2",
    "ollama>=0.6.2",
    "psycopg[binary]>=3.1",
    "pyyaml>=6.0.3",
]
